void main() {
    byte x = 5;
    byte y = 6;

    IO.println(x + y);

    // += add and assign
    // -= subtract and assign
    // *= multiply and assign
    // /= divide and assign

    // && and
    // || or
    // ^ XOR
    // !( && ) NAND
    // !( || ) NOR
}